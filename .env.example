# Optional W&B settings. Do not put real credentials in tracked files.
WANDB_MODE=online
WANDB_PROJECT=modern-transformer-zh-en
WANDB_ENTITY=your-wandb-username-or-team

# Prefer `wandb login` for the API key. If an environment variable is needed,
# define WANDB_API_KEY only in your shell or an ignored local .env file.
